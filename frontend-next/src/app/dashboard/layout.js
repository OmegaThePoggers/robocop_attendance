export const metadata = {
    title: 'Dashboard',
    description: 'Teacher dashboard — manage sessions and attendance',
};

export default function DashboardLayout({ children }) {
    return children;
}
