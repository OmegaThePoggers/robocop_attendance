export const metadata = {
    title: 'Admin Console',
    description: 'Admin dashboard — manage disputes, users, and classes',
};

export default function AdminLayout({ children }) {
    return children;
}
